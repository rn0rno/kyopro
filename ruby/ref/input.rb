#!/usr/bin/env ruby

# 単一入力
gets.chomp!

# 単一入力，複数データ
gets.chomp!.split(" ").map(&:to_i)


# 最初にデータ数が与えられてその回数だけ回す
n = gets.chomp!.to_i

n.times do
end

# 入力された分だけ回す
while line = gets
  a = line.chomp!
end

while line = gets
  d, a = line.chomp!.split(",").map(&:to_i)
end
